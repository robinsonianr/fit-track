meta {
  name: Delete Workout
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}:8080/api/v1/workouts/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  id: 150
}
