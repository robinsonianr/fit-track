meta {
  name: Delete Activity
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}:8080/api/v1/activities/:id
  body: none
  auth: bearer
}

params:path {
  id: 2
}

auth:bearer {
  token: {{bearerToken}}
}
